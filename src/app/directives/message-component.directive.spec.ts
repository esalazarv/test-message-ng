import { MessageComponentDirective } from './message-component.directive';

describe('MessageComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new MessageComponentDirective();
    expect(directive).toBeTruthy();
  });
});
